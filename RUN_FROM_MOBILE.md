# تشغيل ClipBoost Telegram Bot من الجوال (بدون كمبيوتر) 📱

## الخطوة 1: خذ توكن تلغرام (لو ما أخذته)
افتح تطبيق تلغرام → دوّر على **@BotFather** → اكتب `/newbot` → اتبع التعليمات → يعطيك توكن.

## الخطوة 2: افتح Replit
1. روح للمتصفح واكتب: `replit.com`
2. سجّل حساب جديد (بإيميلك أو Google)
3. اضغط **"+ Create Repl"**
4. اختر نوع المشروع: **Python**
5. سمّه: `clipboost-bot`
6. اضغط **Create**

## الخطوة 3: ارفع الملفات
داخل Replit، بالقائمة الجانبية (Files):
1. احذف ملف `main.py` الفاضي (لو موجود)
2. ارفع 3 ملفات من مجلد `telegram-clip-bot`:
   - `bot.py`
   - `video_processor.py`
   - `requirements.txt`

طريقة الرفع: اضغط الثلاث نقاط (⋮) بأعلى قائمة الملفات → **Upload file** → اختر الملفات من جوالك.

## الخطوة 4: حط التوكن بشكل آمن
1. بالقائمة الجانبية اليسار، دوّر أيقونة **قفل 🔒** اسمها **"Secrets"**
2. اضغط **"+ New Secret"**
3. Key: `TELEGRAM_BOT_TOKEN`
4. Value: (الصق التوكن اللي أخذته من BotFather)
5. اضغط **Add Secret**

## الخطوة 5: ثبّت المكتبات
بالأسفل فيه قسم اسمه **"Shell"** (سطر أوامر) — اضغط عليه واكتب:
```bash
pip install -r requirements.txt
```
انتظر لين يخلص التثبيت (قد ياخذ دقيقتين).

## الخطوة 6: شغّل البوت
اضغط الزر الأخضر الكبير **"Run"** فوق.

لو كل شي تمام، بيطلع لك بالأسفل:
```
✅ البوت شغّال... اتركه يشتغل ولا تسكر البرنامج.
```

## الخطوة 7: جرّبه!
روح لتطبيق تلغرام، افتح محادثة مع البوت حقك، أرسل `/start` — لازم يرد عليك فورًا.

بعدها جرب ترسل فيديو مع كابشن بهذا الشكل:
```
هوك: هذا أفضل كليب اليوم
وصف: شوف هذا المقطع الحماسي
هاشتاغ: #gaming #viral #fyp
مدة: 20
```

---

## ⚠️ ملاحظة مهمة: البوت يوقف لو قفلت المتصفح
Replit بالنسخة المجانية يوقف البوت بعد فترة من عدم النشاط. للتجربة كذا تمام. لو تبيه يشتغل 24 ساعة باستمرار، فيه خيار مدفوع بسيط بـ Replit اسمه "Always On" — نقدر نناقشه لاحقًا لو احتجته.
